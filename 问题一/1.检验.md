**已实现的六项模型检验**

1. 量纲一致性：`validate_q1()` 中同时校验 `Q = h·A·ΔT` 与 `Q = ΔT/R_total`。
2. 极限条件：`ΔT=0 -> Q=0、N=0`；`h_air->0 -> N=0`；另做
   `ΔT=10~60 K` 单调性扫描，Q 严格单调增加。
3. 关联式适用域：检查 `Ra=Gr·Pr` 与 `Pr` 是否落在水平圆柱
   Churchill-Chu 关联式的适用范围内。
4. 解析基准：固定 `h=5 W/(m²·K)` 手算 `Q_max=11309.73 W`，
   得到 `N=22`，与模型输出对比。
5. 简化假设：输出稳态传热、薄壳温度均匀、忽略辐射、海水静止的
   适用边界与风险说明。
6. 参数敏感性：对海水温度、限温、h_air、h_sea、q0、k_wall 做
   ±5%/±10% 扰动，并对灵敏度方向做合理性校验；壁厚单独做场景分析。

**1. 输入参数与数据来源核对**

- 检查每个输入参数是否与题面或数据包一致：`D=1 m`、`L=12 m`、壁厚 `w`、`k_wall=14.4 W/(m·K)`（304 不锈钢）、`T_sea=20 ℃`、`q0=500 W`、1U 尺寸 `44.45×482.6×525 mm`。
- 脚本应能自动定位 `C题数据` 文件夹，找不到时打印警告（当前已实现）。
- 建议加一个“参数-来源”对照输出，例如：

|参数|数值|来源|
|---|---|---|
|D, L|1 m, 12 m|题面固定|
|k_wall|14.4 W/(m·K)|材料数据包|
|T_sea|20 ℃|题面固定|
|q0|500 W|题面固定|
|1U 尺寸|44.45×482.6×525 mm|题面固定|

**2. 量纲与公式一致性检查**

- `A_eff` 单位应为 m²，`h` 为 W/(m²·K)，`Q_max` 为 W，`N` 为无量纲整数。
- 检查 `Q_max = h_total × A_eff × ΔT` 是否与 `(T_air_max - T_sea) / R_total` 一致（当前两处计算应相等，可作为断言）。

**3. 物性与换热系数合理性检查**

- 海水物性范围：`rho ≈ 1024~1027 kg/m³`、`k ≈ 0.59~0.60 W/(m·K)`、`mu ≈ 1.0~1.1e-3 Pa·s`、`Pr ≈ 6~8`。
- 换热系数合理范围：`h_air` 一般约 `2~10`，`h_sea` 约 `50~500 W/(m²·K)`，当前结果 `h_air≈5.0`、`h_sea≈232` 符合。
- 综合系数应满足 `h_total < min(h_air, h_sea)`，因为热阻串联。

**4. 壁温与温度约束检查**

- 必须满足 `T_sea < T_wo ≤ T_wi < T_air_max`。薄壳 w=0 时内外壁温相等，当前结果 `20 < 21.3 ≈ 21.3 < 80`，符合。
- 壁温自洽迭代残差应小于容差（当前 `tol=1e-6`，可打印迭代是否收敛、用了多少步）。

**5. 容量逻辑与异常判定检查**

- `N_theory > 0`、`N_space > 0`、`N` 为正整数且 `N ≤ min(N_theory, N_space)`。
- `N_theory < 1` 时强制 `N = 0`（当前已实现）。
- 对边界用例测试：例如把 `h` 调到极小值，确认能正确触发“无法放置服务器”。

**6. 手工测试用例交叉验证**

- 保留现有自检：`h=5、A=πDL、ΔT=60、q0=500、N_space=800 → N=22`，运行时应输出 PASS。
- 用交付清单的“输入输出示例”再核对一遍 `N_theory`、`N_space`、`N`。

**7. 边界扫描（可选）**

- 对 `w` 或 `k_wall` 做小幅扰动，检查结果单调合理（例如壁厚增大→N 不增），这属于敏感性合理性检查。

如果你的需求是把这些检验写进脚本，我可以在 [C题_问题1_服务器散热_整数模型.py](D:/46884/Documents/数学建模/C题_问题1_服务器散热_整数模型.py) 里加一个 `validate_q1()` 校验函数，运行后输出“数据来源核对 + 物理合理性检查 + PASS/FAIL”。需要我现在加上吗？
